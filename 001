#include <stdio.h>
#include <stdlib.h>

int main()
{
    float a;
    scanf("%f",&a);     //вещественныe
    printf("%f",a);

   char str[10] = "";
   printf("");
   fgets(str, 10, stdin);      //строка
   printf("");
   puts(str);

 return 0;
}
