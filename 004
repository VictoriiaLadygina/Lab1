#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a1, a2, bit, b;
    scanf("%d", &a1);
    scanf("%i", &a2);
    b = ((a1 >= 11) && (a1 <= 12))? printf("true\n") : printf("false\n");
    bit = a2 >> 11 & 1;
    printf("%x", bit);
    return 0;
}
